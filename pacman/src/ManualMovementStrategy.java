package src;

import ch.aplu.jgamegrid.Location;

public class ManualMovementStrategy implements IMovementStrategy {
    @Override
    public void move(PacActor pacActor) {
        // The movement is handled by the keyRepeated method in PacActor class
        // No additional logic is needed here
    }
}